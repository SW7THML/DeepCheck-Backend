<div class="navigation-header" align="center">
  <div id="left-menu">
    <%= link_to '', course_path(@course), class: "btn-left btn-primary glyphicon glyphicon-triangle-left" %>
  </div>
  <h3><%= @course_name %></h3>
</div>

<div class="photos">
  <% @post.photos.each do | photo | %>
    <img class="photo" src="<%= photo.attachment %>" />
    <div class="face-grid" data-photo-id="<%= photo.id %>"></div>

    <div class="attendance">
      <label class="col-sm-2 control-label"><span class="glyphicon glyphicon-user"></span> Attendance
      </label>
      <div class="tags">
        <% photo.tagged_users.each do | user | %>
          <div class="tag">
            <%= user %>
          </div>
        <% end %>
      </div>
    </div>
  <% end %>

  <div class="progress">
    <div class="progress-bar progress-bar-striped active" role="progressbar" aria-valuenow="45" aria-valuemin="0" aria-valuemax="100" style="width: 83%">
    83%  
    </div>
  </div>

  <div class="comments">
    <label class="col-sm-2 control-label">Comments</label>
    <div class="content">
      <% @post.comments.each do |comment| %>
        <p>
          <strong>김철수</strong>
          <span><%= comment.content %></span>
          <small class="time"><%= time_ago_in_words(comment.created_at) %> 
          <%= link_to ' ', [comment.post.course, comment.post, comment], class: "ionicons ion-close", method: :delete, data: { confirm: '댓글을 삭제하시겠습니까?'} %>
          </small>
        </p>
      <% end %>
    </div>
  </div>
</div>

<div class="background-comment">
  <div class="group no-image">
    <%= form_for([@course, @post, @post.comments.build]) do |f| %>
      <%= f.text_field :content, class: "form-control", placeholder: 'Content' %>
      <%= f.submit '등록', class: "group btn-comment btn-primary"%>
    <% end %>
  </div>
</div>